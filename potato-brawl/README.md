# 🥔 土豆兄弟·横版（Potato Brawl）

一个可以**在局域网里联机**的 2D 横版动作 Roguelite 网页游戏，自带**权威服务器**（Node.js + WebSocket）。

- **合作 PvE**：4 人一起打 20 波怪物，每 5 波一个 Boss，波间开商店买武器道具，队友倒地可以救
- **PvP**：死斗（FFA）/ 团队战，场上刷武器箱和医疗包，击杀也能升级
- **Brotato 式 build 流**：武器全自动开火，你只管走位；击杀掉经验 → 升级三选一 → 堆出属于自己的流派
- **手感优先的网络同步**：60Hz 服务器模拟 + 客户端预测 + 服务器回滚校正 + 快照插值

---

## 一、快速开始（局域网开黑）

### Windows：双击即可

1. 解压压缩包，进入 `potato-brawl` 目录
2. **双击 `start-cn.bat`**（中文提示）或 **`start.bat`**（英文提示，任何系统都不乱码）
   —— 自动检查 Node.js、装依赖（依赖已内置，离线也能跑）、启动服务端、打开浏览器，并**直接列出本机所有局域网 IP**
3. 把黑窗口里 `http://192.168.x.x:3000` 这样的地址发给同 Wi-Fi 的朋友

> 换端口：`start-cn.bat 8080`。
> 出问题先双击 **`troubleshoot.bat`**（自检 Node/npm/依赖/端口/IP/防火墙，并给出解决办法）。
> 中文说明见 **`GUIDE-CN.txt`**（含完整按键表、玩法、联机排障）。
>
> 兼容性说明：`start.bat` / `troubleshoot.bat` 是**纯 ASCII + CRLF**，压缩包内**所有文件名均为 ASCII**，
> 因此无论系统是中文还是英文、用资源管理器还是 7-Zip 解压，都不会出现乱码或"文件名无效"。

### 其它系统 / 手动启动

```bash
cd potato-brawl
npm install          # 只有一个依赖：ws（压缩包里已内置，可跳过）
npm start            # 等价于 node server/index.js --host 0.0.0.0 --port 3000
```

启动后会打印：

```
  本机访问：   http://localhost:3000
  局域网访问： http://192.168.1.23:3000      ← 把这个发给基友
```

1. 建房间的人打开 `http://localhost:3000`，输入名字 → **创建房间**
2. 其他人用同一个 Wi-Fi，浏览器打开 **`http://192.168.1.23:3000`**（房主机器的局域网 IP）→ 首页会自动列出房间，或输入 4 位房间码加入
3. 全员点「准备」→ 房主点「开始游戏」

> 客户端是服务器一起提供的，基友**不需要装任何东西**，也不需要联网（纯内网）。
> 换端口：`node server/index.js --port 8080`。

### 操作

| 操作 | 按键 |
| --- | --- |
| 移动 | `A`/`D` 或 `←`/`→` |
| 跳跃（可二段跳） | `空格` / `W` / `↑` |
| 冲刺（带无敌帧） | `Shift` |
| 蹲 / 下平台 | `S` / `↓` |
| 救人 | 站在倒地队友身边 3 秒（进度圈走满即救起） |
| 升级三选一 | `1` / `2` / `3` 或鼠标点击 |
| 计分板 | `Tab` |
| 聊天 | `Enter` |
| 属性面板 / 静音 | `C` / `M` |

---

## 二、目录结构

```
potato-brawl/
├── shared/                 # ★ 前后端共用（同一份代码，保证预测一致）
│   ├── constants.js        # 帧率、物理参数、玩家参数、模式/阶段枚举
│   ├── physics.js          # AABB 碰撞、stepActor（客户端预测就跑这个）
│   ├── level.js            # 竞技场（地面/平台/出生点/刷怪口/箱子点）
│   ├── weapons.js          # 12 把武器数值
│   ├── items.js            # 40+ 件道具、属性聚合、升级三选一抽取
│   └── enemies.js          # 敌人数值表 + 波次解锁/难度缩放
├── server/
│   ├── index.js            # 入口：监听端口、打印局域网地址
│   └── src/
│       ├── net/server.js   # HTTP 静态服务 + WebSocket 网关 + 60Hz 主循环
│       ├── rooms/          # RoomManager（建房/加入/列表）与 Room（状态机/消息路由）
│       └── game/
│           ├── game.js     # ★ 权威世界：模拟循环、战斗结算、快照序列化
│           ├── player.js   # 玩家实体、属性计算、自动开火、升级
│           ├── enemies.js  # 敌人 AI（扑跳/冲刺/飞行/远程/坦克/自爆/Boss 三招）
│           ├── nav.js      # ★ 地面怪的跳平台导航：挑跳板 + 二段跳时机（v1.0.4）
│           ├── waves.js    # 波次编排
│           ├── shop.js     # 波间商店
│           └── rng.js      # 可复现随机数（同种子 = 同一局）
├── client/                 # 原生 JS 客户端（无构建、无框架、无外部素材）
│   ├── index.html  css/style.css
│   └── js/ net.js input.js audio.js game.js render.js ui.js main.js
└── test/                   # 无头集成测试 / 浏览器 E2E / 性能基准
```

---

## 三、服务端设计要点

### 1. 权威服务器 + 定步长模拟

- 主循环固定 **60Hz**（`setInterval(4ms)` + 累加器追帧，掉帧最多补 10 tick，避免雪崩）
- 房间内所有实体推进、伤害判定、掉落、AI **全部在服务器算**，客户端只上报输入
- 快照以 **20Hz** 下发，每个玩家单独序列化（因为含个人 HUD/商店/手牌）

### 2. 网络同步：预测 + 回滚 + 插值

| 对象 | 做法 |
| --- | --- |
| 自己的角色 | 客户端本地跑 `shared/physics.js` 的 `stepActor` 预测；收到快照后把自己拉回服务器状态，再用服务器 ack 的 `clientTick` 之后的历史输入重放（reconciliation） |
| 其他角色 / 敌人 / 弹幕 | 两个快照之间做 90ms 延迟插值（lerp），平滑无抖动 |
| 掉落物 | 同上插值，服务器做磁吸吸附 |

为此快照里额外带了两个字段：

- `me.ct`：服务器最后处理的客户端输入序号 → 客户端知道该从哪一帧开始重放
- `me.ph`：`[onGround, jumps, coyote, jumpBuf, dashT, dashCd, iFrames, prevJump, prevDash]` → 让重放的中间状态与服务器完全一致

### 3. 房间与大厅

- 4 位房间码（去掉了易混淆字符）；大厅广播房间列表，首页可一键加入
- 房主可改 **模式 / 地图 / 人数 / 波数 / 难度 / 备战时长 / 分数上限 / 时间上限**，改完立刻广播
- 断线保留 60 秒重连窗口（靠 `token` 重新绑定连接，座位、等级、装备全部保留）
- 中途加入：直接以新手装备进场，下一波自动补齐状态

### 4. 协议（JSON，全部走 `/ws`）

**客户端 → 服务器**

| 类型 | 字段 | 说明 |
| --- | --- | --- |
| `hello` | `name, token` | 握手 / 重连 |
| `create` | `roomName, name` | 建房 |
| `join` | `code, name` | 加入 |
| `leave` | - | 离开 |
| `ready` | `v` | 准备 |
| `settings` | `settings{}` | 房主改设置 |
| `loadout` | `weapon` | 选初始武器 |
| `setTeam` | `team` | 团队战换队 |
| `start` | - | 开始 |
| `input` | `i`（位掩码）, `q`（客户端 tick） | 每帧输入 |
| `pick` | `idx` | 升级三选一 |
| `buy` / `reroll` / `shopReady` | `idx` / - / `v` | 商店 |
| `chat` | `text` | 聊天 |
| `ping` | `ts` | 心跳测延迟 |

**服务器 → 客户端**

| 类型 | 说明 |
| --- | --- |
| `welcome` | 分配 id / token |
| `rooms` | 房间列表 |
| `room` | 房间完整状态（玩家、设置、阶段） |
| `start` | 开局：模式、地图、种子、规则、我的 id |
| `snap` | 20Hz 快照：`p`玩家 `e`敌人 `b`弹幕 `k`掉落 `ev`事件 `me`我的私有数据 |
| `over` | 结算战报 |
| `lobby` | 回到大厅 |
| `chat` / `err` / `pong` | 聊天 / 错误提示 / 心跳回包 |

快照示例（精简）：

```jsonc
{
  "t":"snap", "tick":1234, "ph":"wave", "pt":0, "w":3, "tw":20,
  "p":[{"i":"p_ab12","x":412.3,"y":700,"vx":0,"vy":0,"f":1,"hp":88,"mx":100,"a":1,"d":0,"l":4,"k":7,"w0":"smg"}],
  "e":[{"i":31,"t":"slime","x":900,"y":770,"hp":12,"mx":41,"el":0}],
  "b":[{"i":55,"x":420,"y":690,"vx":900,"vy":0,"r":5,"c":"#ffd36e","k":"bullet","wd":"pistol"}],
  "k":[{"i":61,"t":"xp","x":500,"y":780,"v":3}],
  "ev":[{"t":"hit","x":510,"y":700,"d":14,"cr":1}],
  "me":{ "hp":88,"mx":100,"xp":12,"xn":22,"lv":4,"coins":37,"kills":7,
         "w":[{"id":"smg","cd":0.03}], "it":{"coffee":2}, "ch":null, "sh":null,
         "ct":1201, "ph":[0,1,0,0,0,0.4,0,0,0], "st":{"dmg":1.15,"as":1.2,...} }
}
```

### 5. 玩法系统

- **武器（12 把 / 6 个槽位）**：手枪、拳套、木棍、冲锋枪、霰弹枪、长剑、法杖（追踪）、狙击枪（穿透）、回旋镖（往返穿透）、火箭筒（范围）、电锯（高频）、激光（瞬发射线）
- **道具（40+ 件，4 档稀有度）**：加减伤/攻速/暴击/移速/生命/闪避/吸血/回复/拾取范围/额外弹丸/弹速/射程/经验/金币/幸运/反伤/击杀爆炸，含若干带负面效果的"高风险高回报"件
- **属性聚合**：`computeStats(items)` 把道具折算成最终属性，并对闪避、减伤、攻速等做上限收敛
- **敌人**：史莱姆（扑跳）、疯跑鸡（突进）、飞虫（无视重力）、射手菌（走位放风筝）、石头人（高血高伤）、炸弹怪（贴身引爆）、土豆王 Boss（冲锋 / 跳劈震波 / 召唤小弟，40% 血狂暴）
- **波次**：每波预算 `min(52, 7+2.6×波数) × 难度`，分批入场；每 5 波 Boss；20 波通关
- **合作机制**：经验**全队共享**、金币归击杀者、倒地 25 秒可被救起、全员倒地判负

---

## 四、测试

```bash
npm test                          # 无头集成测试：PvE / FFA / TEAM 三个场景跑通
node test/perf-test.js            # 性能基准：8 人 + 70 怪 + 满弹幕，测 tick 耗时
node test/enemy-jump-test.js      # 敌人跳平台/二段跳回归测试（含稳定性与边界用例）
node test/browser-verify.js       # 真实浏览器 E2E（需要 npx playwright install chromium）
```

实测（普通笔记本 / 沙箱环境，8 人同房间、第 14 波、70 只怪 + 满屏弹幕）：

| 指标 | 数值 |
| --- | --- |
| 平均 tick 耗时 | **0.24 ms**（60Hz 预算 16.67ms 的 1.5%；关掉跳平台导航是 0.21 ms，多出来的约 0.03 ms） |
| p95 / p99 tick | 0.57 ms / 3.3 ms |
| 峰值快照 | 38 KB / 帧 → 8 人约 6 MB/s（局域网无压力） |

---

## 四点五、改动记录

### v1.0.4 · 地面怪会跳上平台追人（二段跳）

**问题**：玩家跳到平台上就绝对安全了 —— 地面怪只共享 `moveAndCollide`，没有任何跳跃逻辑，
`aiHop` 那下 `vy=-560` 是固定节奏的随机蹦，既不看玩家高度也不算落点，结果就是全部堆在平台底下蹭墙，
只有飞行怪能咬到人。

**做法**（`server/src/game/nav.js`）：

1. **跳板规划** `planHop()`：在关卡实心块里挑「跳上去之后离玩家更近」的那块平台，
   同时按 `v²/2g` 算出这跳的顶点高度和滞空时间 —— 只考虑跳得到的平台，
   并检查起跳点/落点站得下、头顶有净空。够不到最高层时会自动挑中间那层当跳板，一级一级爬。
2. **正下方不硬顶**：站在平台正下方起跳只会撞到平台底面，所以计划里带一个 `approachX`，
   怪会先绕到平台侧边（`stepToward` 会优先采用导航给的走位点）再起跳。
3. **二段跳**：空中用当前竖直速度反算预测顶点，顶点不够高就在顶点附近补一段 `PHYS.doubleJumpVel`，
   和玩家的二段跳是同一套参数；`e.jumps` 记段数，落地清零。
4. **上升时把目标平台当柱子**（`moveAndCollide` 新增的 `ledge` 参数）：
   否则斜着插进平台底面会被 Y 轴解算判成"撞天花板"，vy 直接归零，二段跳白送，永远差几十厘米。
   再配一个「差 ≤14px 就直接扒上边缘」的小修正，手感才不会一直差一点。
5. **不会卡死**：撞墙计时（`blockedT`）兜底干跳；没站上目标平台罚 0.42s 冷却防抽搐；
   掉出场地的怪拉回地面。Boss 冲锋/跳劈、炸弹怪引信期间不参与导航，技能节奏不变。

**回归测试**：`node test/enemy-jump-test.js`（44 项：逐怪跳台、多级跳板链、世界边界、无目标/晕眩、Boss）。

**性能**：规划每个怪最多 10 次/秒（`NAV.planEvery`），实测 8 人 70 怪仍是 ~0.27ms/tick。
数值都在 `shared/enemies.js` 的 `jump` 字段（`mul` 跳力倍率、`jumps` 段数）里，想调难度改这里就行。

---

## 五、已知限制 & 可以继续做的

- **快照是明文 JSON 全量**，人多怪多时带宽偏高。要优化可以做：实体增删改的 delta 压缩、数值定点量化、只发视野内实体（当前是整张竞技场可见，所以砍不了视野）。
- **没有做反作弊校验**（速度 hack / 穿墙）。局域网熟人开黑场景够用；若要上公网，需要加服务端输入合法性检查（位移上限、冷却校验）。
- **没有移动端触屏摇杆**，目前是键鼠。
- 可以加的玩法：单人练习（加 AI 陪练 bot）、更多地图与 Boss、天赋/角色选择、观战、录像回放（种子已可复现，回放只需记输入流）。

## 六、技术栈

Node.js 20（原生 ESM）· `ws` · 原生 Canvas 2D · 零前端框架 · 零构建步骤 · 零外部素材（图形和音效全部代码生成）。
